export const metadata = {
  title: 'Your Cart',
  description: 'Review your items and complete your order via WhatsApp.',
};

export default function CartLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}