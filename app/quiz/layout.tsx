export const metadata = {
  title: 'Scent Discovery Quiz',
  description: 'Find your signature fragrance vibe.',
};

export default function QuizLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}